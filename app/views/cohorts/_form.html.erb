
<%= bootstrap_form_with(model: cohort, local: true) do |form| %>
  <% if cohort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cohort.errors.count, "error") %> prohibited this cohort from being saved:</h2>

      <ul>
      <% cohort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.date_select :end_date %>
  </div>

  <div class="field">
    <%= form.collection_select(:course_id, Course.all, :id, :name) %>
  </div>

  <div class="field">
    <%= form.collection_select(:teacher_id, Teacher.all, :id, :full_name) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
